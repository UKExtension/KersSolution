import { Component, ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'app',
    templateUrl: './app.component.html',
    styleUrls: ['./custom.scss', './custom-print.scss'],
    encapsulation: ViewEncapsulation.None,

})
export class AppComponent {
}
