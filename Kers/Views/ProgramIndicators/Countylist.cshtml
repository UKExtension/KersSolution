@{
    Layout = "~/Views/Shared/_LayoutReports.cshtml";
    
    ViewData["Subtitle"] = "Kentucky Extension Reporting System";
    var fy = ViewData["fy"] as string;
    if( fy == "0"){
      fy = "2018";
    }
    ViewData["Title"] = "Program Indicators for FY"+fy;
}
@model List<Kers.Models.Entities.KERScore.PlanningUnit>

<div class="bs-glyphicons">
    <ul class="bs-glyphicons-list">
      <li>
        <a asp-controller="Data"  asp-action="Index">
          <span class="glyphicon glyphicon-menu-left" aria-hidden="true"></span>
          <span class="glyphicon-class">Back to Data</span>
        </a>
      </li>
      <li>
                    <a asp-controller="ProgramIndicators"  asp-action="Get" asp-route-id=0  asp-route-fy="@fy">
                    <span class="glyphicon glyphicon-stats" aria-hidden="true"></span>
                    <span class="glyphicon-class">STATE TOTALS</span>
                    </a>
                </li>
        @if(Model != null){
            @foreach( var unit in Model){
                <li>
                    <a asp-controller="ProgramIndicators"  asp-action="Get" asp-route-id=@unit.Id asp-route-fy="@fy">
                    <span class="glyphicon glyphicon-stats" aria-hidden="true"></span>
                    <span class="glyphicon-class">@unit.Name.Substring(0, @unit.Name.Length - 11)</span>
                    </a>
                </li>
            }
        }
      
      
    </ul>
  </div>